# HyperFrames 编辑器 V4.0

## 1. 素材自动匹配

先导入 SRT，再批量导入素材。系统读取素材文件名，和每一段字幕做关键词匹配，自动生成对应时间片段。为了提高准确率，素材名称尽量包含字幕里出现的关键词，例如 `ChatGPT登录界面.png`。

重新点击自动匹配时，只会替换上一次自动生成的素材层，不会删除手工添加的内容。

## 2. 鼠标教学组件

支持移动、点击、双击、拖拽四种动作。画布位置是起点，右侧属性面板设置终点；底部时间片段越长，鼠标移动越慢。

## 3. HTML 转 MP4

编辑器中的“导出 MP4”会下载一个 `.render.html` 文件。把它拖到项目文件夹中的 `HTML转MP4.bat` 上，程序会调用电脑里的 Edge 或 Chrome 逐帧渲染，并自动合成配音。

第一次转换会自动准备所需组件，耗时会比以后更长。

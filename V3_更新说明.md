# HyperFrames 编辑器 V3.0 更新说明

这次更新优先完成生产闭环，没有继续增加视觉特效。

## 已完成

- 自动保存：页面刷新后恢复上次工程。
- 素材持久化：图片、视频随 JSON 工程保存，不再使用刷新即失效的临时地址。
- 自由移动缩放：视频默认不再强制铺满背景；图片和视频可拖动，选中后拖右下角圆点缩放。
- 配音轨：顶栏“导入配音”，时间轴显示配音长度并与播放头同步。
- SRT 场景轨：顶栏“导入 SRT”，自动生成场景和逐句字幕。
- HyperFrames HTML：顶栏“导出 HTML”，得到可独立打开的自包含工程。
- JSON 工程升级：工程名称、配音、场景、字幕和素材统一保存。

## 使用顺序

1. 导入配音。
2. 导入对应的 SRT。
3. 拖入截图、图片或视频。
4. 调整位置、大小和时间。
5. 导出 JSON 备份工程，导出 HTML 进入渲染环节。

`examples/quick-test.srt` 可以用来快速检查场景时间轴。

## 下一阶段

- 文案与本地素材自动匹配。
- 浏览器窗口、鼠标移动、点击反馈、矩形高亮和局部放大。
- HTML 逐帧渲染并合成为 MP4。
